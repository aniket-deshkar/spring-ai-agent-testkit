# Contributing

Open an issue before proposing a public API change. For code changes, create a focused branch, add tests for behavior and failure semantics, then run:

```shell
./mvnw verify
```

Use concise conventional commit messages such as `feat: add transcript assertion` or `fix: preserve scripted failure cause`.
